{namespace ElectricReadingProgress}

/**
 */
{template .render}
	{@param? elementClasses: ?}

	<div class="affix-top{$elementClasses ? ' ' + $elementClasses: ''}">
		<div ref="readingContainer"></div>
	</div>
{/template}
